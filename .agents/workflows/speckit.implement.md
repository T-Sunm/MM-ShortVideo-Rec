# SpecKit: Implement
## Goal
Execute the changes based on the approved plan.

## Protocol
1. Follow the plan in `.agents/rules/planning.md`.
2. Apply changes in small, logical commits/diffs.
3. Ensure no guardrails in `.agents/rules/guardrails.md` are violated.
4. Verify using `skills/recommender-debug/SKILL.md`.
